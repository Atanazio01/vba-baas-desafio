# Web — React + Vite

Frontend do lojista e checkout público.

**Documentação completa:** veja o [README na raiz do repositório](../README.md).

```bash
pnpm install
pnpm dev
```

Requer `VITE_API_URL` apontando para a API (ex.: `http://localhost:3000`).
