# API — NestJS BaaS

Backend da aplicação BaaS (NestJS + TypeORM + MySQL).

**Documentação completa:** veja o [README na raiz do repositório](../README.md).

```bash
pnpm install
pnpm start:dev
```

Swagger local: http://localhost:3000/docs
